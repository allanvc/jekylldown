% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{set_theme_color}
\alias{set_theme_color}
\title{Set the al-folio accent color}
\usage{
set_theme_color(dir = ".", color)
}
\arguments{
\item{dir}{Site root.}

\item{color}{A named theme color (e.g. \code{"red"}, \code{"blue"}, \code{"green"},
\code{"cyan"}, \code{"purple"}, \code{"pink"}, \code{"orange"}, \code{"yellow"}) or a CSS hex
value like \code{"#b71c1c"}.}
}
\value{
The hex color applied, invisibly.
}
\description{
al-folio derives its accent (links, buttons, badges) from the
\code{--global-theme-color} CSS variable defined in the theme's
\verb{_sass/_themes.scss} -- purple in light mode, cyan in dark mode. This
function overrides it without touching the theme: the theme's
\code{assets/css/main.scss} is copied into the site (a local file shadows the
theme's) and a plain-CSS override block is appended, one rule per
selector in which the theme sets \code{--global-theme-color}, so the override
tracks the theme's own light/dark structure. Named colors are resolved
from the theme's palette (\verb{$red-color}, \verb{$blue-color}, ... in
\verb{_sass/_variables.scss}); hex values are used as-is.
}
\details{
The theme files live in the \code{al_folio_core} gem, so run
\code{\link[=bundle_install]{bundle_install()}} once before calling this. Calling it again replaces
the previous override, so the color can be changed at any time.

On a Chirpy site this delegates to \code{set_theme_style(accent = color)}
(the link color); on Minimal Mistakes it points you at
\code{\link[=set_theme_skin]{set_theme_skin()}} instead.
}
\examples{
\dontrun{
set_theme_color("my-site", "red")
set_theme_color("my-site", "#0057b7")
}
}
