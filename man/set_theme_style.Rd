% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style.R
\name{set_theme_style}
\alias{set_theme_style}
\title{Customize theme colors through the theme's own CSS variables}
\usage{
set_theme_style(..., dir = ".")
}
\arguments{
\item{...}{Named style options from the list above.}

\item{dir}{Site root, or any directory inside it -- like
\code{\link[=build_site]{build_site()}}, the function climbs to the enclosing site, so the
default \code{"."} works from anywhere in the site's project.}
}
\value{
Invisibly, the named list of CSS variables written.
}
\description{
al-folio and Chirpy define their palettes as CSS variables, with one
block per color scheme (light and dark). This function overrides those
variables from R -- the most robust customization layer, because the
theme's components all read from the variables, and light/dark mode
keeps working.
}
\details{
Every option takes either a single color (applied to both modes) or a
vector like \code{c(light = "#ffffff", dark = "#1c1c1d")}. Colors can be a
\verb{#hex} value or any CSS color (on al-folio, also a theme palette name
like \code{"red"} or \code{"blue"}). Re-running replaces the previous override.

Options on \strong{al-folio}: \code{accent} and \code{hover} (the accent color --
\code{\link[=set_theme_color]{set_theme_color()}} is shorthand for both), \code{background}, \code{text},
\code{text_light}, \code{divider}, \code{highlight}, \code{code_background},
\code{card_background}, \code{footer_background}, \code{footer_text}, \code{footer_link}.

Options on \strong{Chirpy}: \code{accent} (the link color), \code{background}, \code{text},
\code{text_light}, \code{heading}, \code{divider}, \code{code_background},
\code{card_background}, \code{sidebar_background}, \code{sidebar_active}.

\strong{Minimal Mistakes} has no CSS variables (its palette is compiled from
Sass skins): use \code{\link[=set_theme_skin]{set_theme_skin()}} to switch skins, and
\code{\link[=set_element_style]{set_element_style()}}/\code{\link[=add_css]{add_css()}} for finer control. The same applies
to minima.

This needs the theme gems: run \code{\link[=bundle_install]{bundle_install()}} once first.
}
\examples{
\dontrun{
# from anywhere inside the site's project:
set_theme_style(
  accent = "red",
  footer_background = "#222222",
  background = c(light = "#fffdf7", dark = "#1c1c1d")
)

# from outside, name the site:
set_theme_style(accent = "red", dir = "my-site")
}
}
