% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{bundle_install}
\alias{bundle_install}
\title{Install a site's gem dependencies with Bundler}
\usage{
bundle_install(dir = ".")
}
\arguments{
\item{dir}{Directory in (or under) the site.}
}
\value{
Invisibly, the \code{\link[processx:run]{processx::run()}} result.
}
\description{
Runs \verb{bundle install} in the site root with the package's isolated gem
environment injected, so a theme's \code{Gemfile} (al-folio has one) resolves
without any manual \code{GEM_HOME}/\code{PATH} setup. Needed once per site (and
after editing the \code{Gemfile}); afterwards \code{\link[=build_site]{build_site()}} and
\code{\link[=serve_site]{serve_site()}} automatically switch to \verb{bundle exec jekyll}.
}
\examples{
\dontrun{
bundle_install("my-site")
build_site("my-site")   # now builds with `bundle exec jekyll`
}
}
