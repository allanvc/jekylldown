% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deploy.R
\name{use_pages_workflow}
\alias{use_pages_workflow}
\title{Add a GitHub Pages deploy workflow to the site}
\usage{
use_pages_workflow(dir = ".")
}
\arguments{
\item{dir}{Site root.}
}
\value{
Invisibly, the path of the workflow file (or of the theme's
own workflow when one already exists).
}
\description{
Writes the standard GitHub Actions workflow that builds the site with
Jekyll and publishes it to GitHub Pages on every push
(\code{.github/workflows/jekyll.yml}): checkout, Ruby with cached bundle,
\verb{jekyll build}, upload, deploy. After pushing, enable it once in the
repository settings: \emph{Settings > Pages > Source: GitHub Actions}.
}
\details{
al-folio and Chirpy already ship their own deploy workflow -- for
those sites this function leaves it alone and tells you so. It is
meant for the themes that do not (minima, Minimal Mistakes, most
others).

Note the division of labor: the workflow only runs \verb{jekyll build}.
Knitting stays local -- run \code{\link[=build_site]{build_site()}} (with
\code{local_jekyll = FALSE} if you have no local Jekyll), then commit both
\verb{_source/} and the generated \verb{_posts/*.md} and push.

The workflow caches gems, which requires a committed \code{Gemfile.lock}:
run \code{\link[=bundle_install]{bundle_install()}} once and commit the lockfile it produces.
}
\examples{
\dontrun{
use_pages_workflow("my-site")
}
}
