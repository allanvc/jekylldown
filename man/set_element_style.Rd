% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style.R
\name{set_element_style}
\alias{set_element_style}
\title{Style one semantic element of the theme}
\usage{
set_element_style(
  dir = ".",
  element,
  color = NULL,
  background = NULL,
  size = NULL,
  ...
)
}
\arguments{
\item{dir}{Site root.}

\item{element}{One of the element names above.}

\item{color, background, size}{Text color, background color and
\code{font-size} for the element. Colors accept theme palette names,
\verb{#hex}, or any CSS color.}

\item{...}{Further CSS properties as named arguments, with \verb{_} for
\code{-}: e.g. \code{font_weight = "600"}, \code{border_bottom = "none"}.}
}
\value{
Invisibly, the CSS selector styled.
}
\description{
Styles a single part of the site -- the navbar, the footer, headings --
without writing CSS: the element name is translated to the theme's own
selectors and the properties are appended to the site-local stylesheet
in a managed block (one per element; re-running replaces it).
}
\details{
Elements: \code{"navbar"}, \code{"brand"} (the site name in the navbar),
\code{"footer"}, \code{"headings"}, \code{"post_title"}, \code{"links"}, \code{"code"},
\code{"buttons"}.
}
\section{A word of caution -- this is the fragile layer}{

Unlike \code{\link[=set_theme_style]{set_theme_style()}}, which goes through the CSS variables the
theme itself promises, this layer depends on a curated map of theme
selectors (\code{.navbar}, \code{footer}, ...) that upstream can rename at any
release. jekylldown checks the installed theme's sass and warns when a
mapped selector is no longer found, but the check needs the theme gems
installed and cannot catch everything. Prefer \code{\link[=set_theme_style]{set_theme_style()}}
whenever a CSS variable covers your case; treat this function as the
middle ground before dropping to \code{\link[=add_css]{add_css()}}.
}

\examples{
\dontrun{
set_element_style("my-site", "navbar",
                  background = "#222222", color = "white")
set_element_style("my-site", "headings", color = "red",
                  font_weight = "600")
}
}
