% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.R
\name{build_site}
\alias{build_site}
\title{Knit sources and build the site}
\usage{
build_site(dir = ".", local_jekyll = NULL, force = FALSE)
}
\arguments{
\item{dir}{Directory in (or under) the site.}

\item{local_jekyll}{Run \verb{jekyll build} after knitting? \code{NULL} (default)
auto-detects: builds when a \code{jekyll} executable is found, otherwise
skips with a note (knitting alone is enough when GitHub Pages/Actions
does the Jekyll build remotely).}

\item{force}{Re-knit sources even when the output is up to date?}
}
\value{
Invisibly, the paths of the posts (re)knitted.
}
\description{
Knits every outdated \verb{_source/*.Rmd} and renders every outdated
\verb{_source/*.qmd} (Quarto) to \verb{_posts/*.md} (see \code{\link[=knit_post]{knit_post()}}), then runs
\verb{jekyll build} if a usable Jekyll is available.
}
\examples{
\dontrun{
# knit/render outdated posts, then build with the local Jekyll (if any)
build_site("my-site")

# knit only -- e.g. when GitHub Actions does the Jekyll build remotely
build_site("my-site", local_jekyll = FALSE)

# re-knit everything from scratch
build_site("my-site", force = TRUE)
}
}
