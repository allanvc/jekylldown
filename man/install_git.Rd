% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{install_git}
\alias{install_git}
\title{Install a portable git into jekylldown's isolated toolchain (Windows)}
\usage{
install_git(file = NULL, version = NULL, force = FALSE)
}
\arguments{
\item{file}{Optional path to an already-downloaded
\verb{MinGit-<version>-64-bit.zip}, for offline installs. Default \code{NULL}
downloads the latest release from GitHub.}

\item{version}{Optional Git for Windows release to install instead
of the latest, in asset notation (e.g. \code{"2.55.0.3"} for the tag
\code{v2.55.0.windows.3}, \code{"2.47.1"} for \code{v2.47.1.windows.1}).}

\item{force}{Re-download even when the toolchain directory already
holds a working git? Default \code{FALSE}.}
}
\value{
The path to the installed \code{git} executable, invisibly.
}
\description{
The one jekylldown step that needs git is \code{\link[=bundle_install]{bundle_install()}} on a
\code{Gemfile} that pulls gems straight from git repositories (al-folio's
does) -- Bundler shells out to git for those. This function covers
machines without git: it downloads MinGit, the portable build that
Git for Windows publishes exactly for embedding (a plain \code{.zip}, no
installer, no registry entries, no admin rights), and unpacks it
under \code{tools::R_user_dir("jekylldown", "data")/git}, where every
jekylldown-run Ruby command finds it automatically. Nothing is
installed system-wide; delete the data directory to uninstall.
}
\details{
Like \code{\link[=install_ruby]{install_ruby()}}, the release lookup uses the GitHub API and
falls back to the \code{releases/latest} page on \code{github.com} (then to a
pinned known-good release) on networks where \code{api.github.com} is
unreachable. x86-64 only.

This is a private copy for the toolchain: it does not appear on your
system \code{PATH}, so it does not replace a real git installation for
everyday version-control work (for publishing a site with
\verb{git push}, install git from \url{https://git-scm.com/downloads}).
}
\examples{
\dontrun{
install_git()

# offline: point at an archive downloaded elsewhere
install_git(file = "C:/Users/me/Downloads/MinGit-2.55.0.3-64-bit.zip")
}
}
