% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.R
\name{knit_post}
\alias{knit_post}
\title{Knit one source post to Jekyll-ready Markdown}
\usage{
knit_post(input, output, root = NULL, method = NULL)
}
\arguments{
\item{input}{Path to the \code{.Rmd} (or \code{.qmd}) source.}

\item{output}{Path of the \code{.md} to write.}

\item{root}{Site root; found from \code{input} when \code{NULL}.}

\item{method}{For \code{.Rmd} sources: \code{"knitr"} (default) knits with plain
knitr -- fast, no pandoc needed; \code{"pandoc"} renders through
rmarkdown/pandoc, enabling citations (\verb{bibliography:}), footnotes
and cross-references. \code{NULL} (the default) takes the post's
\verb{knit_method:} front matter key if present, then
\code{options(jekylldown.knit_method =)}, then \code{"knitr"}. Ignored for
\code{.qmd} (always the Quarto CLI).}
}
\value{
\code{output}, invisibly.
}
\description{
Knits a single \code{.Rmd} with the conventions inherited from Yihui Xie's
knitr-jekyll: front matter preserved, output fenced for kramdown/rouge
via \code{\link[knitr:output_hooks]{knitr::render_jekyll()}}, figures written to
\verb{assets/img/posts/<post-name>/} and referenced through
\code{{{ site.baseurl }}} so they resolve wherever the site is mounted.
}
\details{
A \code{.qmd} input is rendered with the Quarto CLI (\verb{quarto render --to gfm}) instead, and the result is adapted to the same conventions: the
front matter of the \code{.qmd} is carried over (minus Quarto-only keys,
with \code{layout: post} added), and figures are moved to
\verb{assets/img/posts/<post-name>/}. Quarto is looked up in jekylldown's
isolated toolchain (see \code{\link[=install_quarto]{install_quarto()}}), then on the \code{PATH};
\code{options(jekylldown.quarto = "/path/to/quarto")} overrides both.

In both cases Markdown pipe tables get a \verb{\{: .table\}} kramdown IAL so
the theme styles them (al-folio only styles Bootstrap's \code{.table}
class; a bare \verb{<table>} looks unstyled).

Mostly called for you by \code{\link[=build_site]{build_site()}} and \code{\link[=serve_site]{serve_site()}} (the latter via
the generated \code{build.R}), but exported so those entry points and users can
share one implementation.
}
\examples{
\dontrun{
# normally called for you by build_site()/serve_site()
knit_post("my-site/_source/2026-01-01-hello.Rmd",
          "my-site/_posts/2026-01-01-hello.md")

# force the pandoc pipeline for one post (citations, footnotes, ...)
knit_post("my-site/_source/2026-01-01-paper.Rmd",
          "my-site/_posts/2026-01-01-paper.md", method = "pandoc")
}
}
