% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serve.R
\name{serve_site}
\alias{serve_site}
\title{Serve the site locally with live reload}
\usage{
serve_site(dir = ".", background = interactive(), port = NULL, ...)
}
\arguments{
\item{dir}{Directory in (or under) the site.}

\item{background}{Run the watcher/server in a separate R process?
Default \code{TRUE} in interactive sessions.}

\item{port}{Port to serve on; a random free port by default.}

\item{...}{Passed on to \code{\link[servr:httd]{servr::httw()}} (e.g. \code{daemon}, \code{interval})
when \code{background = FALSE}.}
}
\value{
Invisibly, the preview URL.
}
\description{
Builds the site and serves \verb{_site/} with live reload: keep the
preview tab open in the browser and every save of a watched file --
\verb{_source/*.Rmd} and \verb{*.qmd}, Markdown pages, \verb{_config.yml}, data
files, styles -- re-knits what is stale, reruns \verb{jekyll build} and
reloads the page. No manual \code{\link[=build_site]{build_site()}} calls while writing.
}
\details{
By default the whole thing runs in a \strong{separate background R
process}: your console stays free, nothing is printed there, and
rebuild output goes to a log file (\code{.jekylldown-serve.log} in the
site; the path is shown when the server starts). Stop it with
\code{\link[=stop_server]{stop_server()}}. With \code{background = FALSE} the server runs in this
session instead (as a servr daemon), which keeps the console usable
but briefly busy during each rebuild.

The rebuild is triggered by the open preview tab (it pings the server
about once a second); with no browser tab open, nothing rebuilds.
}
\examples{
\dontrun{
serve_site("my-site")               # console stays free; open the URL
stop_server("my-site")
}
}
