% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style.R
\name{add_css}
\alias{add_css}
\title{Add (or remove) a managed block of custom CSS}
\usage{
add_css(dir = ".", css, id = "custom")
}
\arguments{
\item{dir}{Site root.}

\item{css}{Character vector of CSS lines. \code{character(0)} (or \code{""})
removes the block with this \code{id}.}

\item{id}{Identifier of the block (lowercase letters, digits, \code{-},
\verb{_}), so independent customizations do not overwrite each other.}
}
\value{
Invisibly, the path of the stylesheet written.
}
\description{
The escape hatch under \code{\link[=set_theme_style]{set_theme_style()}}, \code{\link[=set_theme_font]{set_theme_font()}} and
\code{\link[=set_element_style]{set_element_style()}}: free-form CSS, but written into a
marker-delimited block in the site-local stylesheet, so it is
idempotent per \code{id}, easy to find, and removable -- instead of hand
edits scattered through theme files. Works with any theme whose site
has (or can get) a local \code{main.scss}.
}
\examples{
\dontrun{
add_css("my-site", c(
  ".profile img { border-radius: 50\%; }"
), id = "round-avatar")

# remove it later
add_css("my-site", character(0), id = "round-avatar")
}
}
