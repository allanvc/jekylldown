% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style.R
\name{set_theme_font}
\alias{set_theme_font}
\title{Customize the site fonts}
\usage{
set_theme_font(
  dir = ".",
  family = NULL,
  size = NULL,
  headings = NULL,
  code = NULL,
  google = TRUE
)
}
\arguments{
\item{dir}{Site root.}

\item{family}{Body font family (e.g. \code{"Lora"}). With \code{google = TRUE}
the face is fetched from Google Fonts at call time (needs network);
otherwise it must be available on the visitor's system.}

\item{size}{Base font size as a CSS length (\code{"17px"}, \code{"110\%"}, ...).
Themes sized in \code{rem} (al-folio, minima) scale the whole site.}

\item{headings}{Font family for \code{h1}-\code{h6} (defaults to \code{family}'s
behavior -- i.e. unchanged unless given).}

\item{code}{Font family for code (\code{code}, \code{pre}, \code{kbd}, \code{samp}).}

\item{google}{Fetch the given families from Google Fonts and inline
their \verb{@font-face} rules? Default \code{TRUE}.}
}
\value{
Invisibly, the path of the stylesheet written.
}
\description{
Overrides the theme's font family and base size without touching the
theme: \verb{@font-face} rules for Google Fonts are inlined into the
site-local stylesheet (they are valid anywhere in a stylesheet, unlike
\verb{@import}), followed by \code{font-family}/\code{font-size} overrides. Re-running
replaces the previous override.
}
\examples{
\dontrun{
set_theme_font("my-site", family = "Lora", size = "17px")
set_theme_font("my-site", code = "JetBrains Mono")
}
}
