% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/migrate.R
\name{migrate_hugo}
\alias{migrate_hugo}
\title{Migrate a Hugo/blogdown site to Jekyll (best effort)}
\usage{
migrate_hugo(
  from,
  to,
  theme = c("minima", "al-folio", "chirpy", "minimal-mistakes"),
  drafts = TRUE,
  only_referenced = FALSE,
  publications = c("bib", "html"),
  theme_color = NULL,
  rerender = TRUE
)
}
\arguments{
\item{from}{Root of the existing Hugo site (must contain \verb{content/}).}

\item{to}{Directory to create the Jekyll site in (must not exist or be
empty).}

\item{theme}{Passed to \code{\link[=new_site]{new_site()}}.}

\item{drafts}{Migrate draft posts (as \code{published: false})? Default \code{TRUE}.}

\item{only_referenced}{Copy from \verb{static/} only the files referenced by
the migrated content (posts, pages, config)? Default \code{FALSE} (copy
everything, as Hugo serves it).}

\item{publications}{How to migrate a publications page found in the menu
(slug \code{publications}, \code{papers} or \code{pubs}). \code{"bib"} (default) extracts
the DOIs from the page, fetches each reference as BibTeX from
\url{https://doi.org} (Crossref/DataCite content negotiation; needs
network), pairs preview images found next to each DOI, and writes
\verb{_bibliography/papers.bib} so al-folio's jekyll-scholar renders the
publications page with the theme's badges and buttons. Entries
without a DOI are reported for manual addition. On themes without
jekyll-scholar (minima, Chirpy, Minimal Mistakes) the page is
rebuilt instead as a clean Markdown list of formatted citations
(APA, also from doi.org) -- raw-HTML publication pages rarely
survive kramdown. \code{"html"} keeps your page as-is; \code{"bib"} falls
back to it automatically when the page has no DOI or there is no
network.}

\item{theme_color}{Accent color for al-folio, passed to
\code{\link[=set_theme_color]{set_theme_color()}} (a palette name like \code{"red"} or a \verb{#hex} value).
Applied right away when the theme gems are already installed;
otherwise remembered in the report as a step to run after
\code{\link[=bundle_install]{bundle_install()}}. Default \code{NULL} keeps the theme default.}

\item{rerender}{Should \code{.Rmd}/\code{.Rmarkdown} sources be migrated to
\verb{_source/} for re-knitting (\code{TRUE}, default)? With \code{FALSE}, when a
post's committed rendered output (\code{.markdown}/\code{.md}) sits next to
its R source, the rendered file is migrated as a plain post instead
-- the right choice for archival migrations, where years of old
posts should not have their code executed again.}
}
\value{
Invisibly, a list report: converted posts, drafts, pages, files
with remaining shortcodes, config keys set, social links written,
static files skipped, and content left for manual migration.
}
\description{
Creates a fresh Jekyll site with \code{\link[=new_site]{new_site()}} and converts the mechanical
parts of a Hugo site into it. The Hugo config (\code{config.toml}/\code{.yaml}) is
read first and drives the migration:
}
\details{
\itemize{
\item posts under \verb{content/post/}, \verb{content/posts/} or \verb{content/blog/} are
renamed to Jekyll's \code{YYYY-MM-DD-slug} convention; \code{.Rmd}/\code{.Rmarkdown}
sources go to \verb{_source/} (to be knitted by jekylldown), plain
\code{.md}/\code{.markdown} to \verb{_posts/};
\item every page referenced by the Hugo menu (\verb{[[menu.main]]}) is migrated
to the theme's own convention, with \code{permalink} preserving the Hugo
URL and the menu order kept: al-folio gets \verb{_pages/<slug>.md} with
\code{nav_order}, Chirpy gets \verb{_tabs/<slug>.md} with \code{order}, Minimal
Mistakes gets \verb{_pages/<slug>.md} plus an entry in
\verb{_data/navigation.yml}, minima gets \verb{<slug>.md} at the root; a menu
entry pointing at the posts section maps to the theme's blog page;
\item the home page (\verb{content/_index.md}) becomes the body of the theme's
landing page (al-folio: \verb{_pages/about.md}; Chirpy: \verb{_tabs/about.md};
Minimal Mistakes and minima: \code{index.md}); an avatar
\verb{\{\{< figure class="avatar" >\}\}} becomes the theme's profile picture
(al-folio profile, Chirpy \verb{avatar:}, Minimal Mistakes
\code{author.avatar});
\item site identity from the config (title/author, \code{baseURL}, description)
is written into the new \verb{_config.yml} (al-folio: \code{first_name}/
\code{last_name}, jekyll-scholar author names; minima: \code{title});
\item social profile links found in the Hugo pages (GitHub, LinkedIn, ORCID,
Google Scholar, ResearchGate, StackOverflow, ...), the first e-mail
address and a link to a CV/resume PDF are written where the theme
shows them: al-folio's \verb{_data/socials.yml}, Chirpy's \verb{social:} config
block (plus its github/twitter buttons), or Minimal Mistakes'
\verb{author:} sidebar links. Posts are not scanned (they may link to
other people's profiles), and only profile-shaped URLs count (a
GitHub repository link is ignored);
\item front matter (YAML \verb{---} or TOML \verb{+++}) is converted to Jekyll YAML:
\code{layout: post} added, \code{draft: true} becomes \code{published: false},
\code{aliases} becomes \code{redirect_from} (needs the jekyll-redirect-from
plugin), Hugo-only keys are dropped;
\item \verb{static/} is copied to the site root, so absolute asset URLs like
\verb{/images/foo.png} keep working; with \code{only_referenced = TRUE} only the
files actually referenced by the migrated content are copied;
\item page-bundle resources (\code{content/post/my-post/plot.png}) are copied to
\verb{assets/img/posts/<post>/} and relative image links are rewritten;
\item \verb{\{\{< figure >\}\}} and \verb{\{\{< youtube >\}\}} shortcodes are converted;
any other shortcode is left in place and reported for manual porting.
}

The Hugo site is only read, never modified. Content not referenced by the
menu, layouts and whatever else cannot be converted is listed in the
final report.
}
\examples{
\dontrun{
migrate_hugo(
  from  = "~/blog-hugo",     # existing blogdown/Hugo site (never written to)
  to    = "~/blog-jekyll",   # created here; must not exist yet
  theme = "al-folio",
  only_referenced = TRUE,    # copy only the static files actually used
  theme_color = "red"
)

# then preview locally:
bundle_install("~/blog-jekyll")
build_site("~/blog-jekyll")
serve_site("~/blog-jekyll")
}
}
