% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/new_site.R
\name{new_site}
\alias{new_site}
\title{Create a new Jekyll site}
\usage{
new_site(
  dir,
  theme = c("minima", "al-folio", "chirpy", "minimal-mistakes"),
  title = "A jekylldown site",
  sample = TRUE,
  demo = TRUE
)
}
\arguments{
\item{dir}{Directory to create the site in. Must be empty or nonexistent.}

\item{theme}{\code{"minima"} (default), \code{"al-folio"}, \code{"chirpy"} or
\code{"minimal-mistakes"}.}

\item{title}{Site title written to \verb{_config.yml} (all themes except
al-folio, which builds its identity from first/last name -- edit its
config, or let \code{\link[=migrate_hugo]{migrate_hugo()}} fill it).}

\item{sample}{Create a sample \code{.Rmd} post in \verb{_source/}? Default \code{TRUE}.}

\item{demo}{Keep the theme's demo content? Only al-folio ships demo
content (sample posts, news, projects, books and an example
biography); default \code{TRUE}, set to \code{FALSE} for a site that starts
empty. \code{\link[=migrate_hugo]{migrate_hugo()}} always starts from a scrubbed site. The other
themes start empty regardless.}
}
\value{
The normalized site path, invisibly.
}
\description{
Scaffolds a Jekyll site ready for the jekylldown workflow: R Markdown
posts live in \verb{_source/}, are knitted to \verb{_posts/*.md}, and figures land
in \verb{assets/img/posts/}. Four themes are supported first-class:
}
\details{
\itemize{
\item \code{"minima"} (default) -- a minimal self-contained blog, generated
locally (no network needed);
\item \code{"al-folio"} -- the academic theme
(\url{https://github.com/alshedivat/al-folio}), cloned with git;
\item \code{"chirpy"} -- the technical-blog theme
(\url{https://github.com/cotes2020/jekyll-theme-chirpy}): its starter
template is cloned with git and stripped of the upstream repo
tooling, keeping the GitHub Pages deploy workflow;
\item \code{"minimal-mistakes"} -- the general-purpose theme
(\url{https://github.com/mmistakes/minimal-mistakes}), generated
locally as a gem-based site (see \code{\link[=set_theme_skin]{set_theme_skin()}} for its skins).
}
}
\examples{
\dontrun{
# a minimal blog with the locally generated minima theme (no network)
new_site("my-blog")

# the al-folio academic theme, cloned from GitHub, starting empty
new_site("my-site", theme = "al-folio", demo = FALSE)

# the Chirpy starter, or a gem-based Minimal Mistakes site
new_site("my-blog", theme = "chirpy")
new_site("my-blog", theme = "minimal-mistakes", title = "My blog")
}
}
