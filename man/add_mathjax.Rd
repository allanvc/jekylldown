% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style.R
\name{add_mathjax}
\alias{add_mathjax}
\title{Enable LaTeX math rendering (MathJax)}
\usage{
add_mathjax(dir = ".")
}
\arguments{
\item{dir}{Site root, or any directory inside it -- like
\code{\link[=build_site]{build_site()}}, the function climbs to the enclosing site, so the
default \code{"."} works from anywhere in the site's project.}
}
\value{
Invisibly, a short description of what was changed.
}
\description{
R Markdown writers use LaTeX math, but not every Jekyll theme renders
it out of the box (on minima, \verb{$$...$$} shows up as literal text).
This turns math on the way each theme expects:
}
\details{
\itemize{
\item \strong{al-folio}: sets its native \code{enable_math: true};
\item \strong{Chirpy}: enables its per-page \code{math} flag site-wide through a
front matter default;
\item \strong{Minimal Mistakes}: loads MathJax through the theme's
\code{head_scripts} hook;
\item \strong{minima} (and unknown themes): copies the theme's
\verb{_includes/head.html} into the site and inserts the MathJax
\verb{<script>} before \verb{</head>}, in a managed, idempotent block.
}

kramdown (Jekyll's Markdown engine) emits MathJax-v2-flavored output
for math, so the v2 bundle is loaded where jekylldown injects the
script itself; al-folio and Chirpy ship their own MathJax setups.
}
\examples{
\dontrun{
add_mathjax("my-site")
}
}
