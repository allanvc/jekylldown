% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{install_ruby}
\alias{install_ruby}
\title{Install Ruby and Jekyll into jekylldown's isolated toolchain (Windows)}
\usage{
install_ruby(file = NULL, devkit = FALSE)
}
\arguments{
\item{file}{Optional path to an already-downloaded
\verb{rubyinstaller-<version>-x64.7z}, for offline installs. Default
\code{NULL} downloads the latest release from GitHub.}

\item{devkit}{Also include the MSYS2 toolchain, needed only for gems
that compile C extensions on install? Default \code{FALSE}.}
}
\value{
The path to the installed \code{jekyll} command, invisibly.
}
\description{
Windows has no system package manager to get Ruby from, so this
function does the whole setup: it downloads the portable RubyInstaller
archive (a \verb{.7z}, no graphical installer, no registry entries), unpacks
it under \code{tools::R_user_dir("jekylldown", "data")/ruby}, and installs
the \strong{jekyll} and \strong{bundler} gems into the package's isolated gem
directory. Nothing is installed system-wide and no shell configuration
is touched; uninstall by deleting the data directory. \code{\link[=build_site]{build_site()}},
\code{\link[=serve_site]{serve_site()}} and \code{\link[=check]{check()}} find this toolchain automatically.
}
\details{
Unpacking the \verb{.7z} archive needs the \pkg{archive} package
(\code{install.packages("archive")}).

Most gems jekylldown's themes use ship precompiled Windows binaries.
If a \verb{gem install}/\code{\link[=bundle_install]{bundle_install()}} ever fails complaining about a
missing compiler ("you have to install development tools first"),
re-run with \code{devkit = TRUE} to get the larger archive that bundles the
MSYS2 build tools.

On Linux and macOS install Ruby with your package manager instead (see
the README's prerequisites); jekylldown picks it up from the \code{PATH}.
}
\examples{
\dontrun{
install_ruby()

# offline: point at an archive downloaded elsewhere
install_ruby(file = "C:/Users/me/Downloads/rubyinstaller-3.3.7-1-x64.7z")
}
}
