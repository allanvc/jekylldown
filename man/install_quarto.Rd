% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{install_quarto}
\alias{install_quarto}
\title{Install the Quarto CLI into jekylldown's isolated toolchain}
\usage{
install_quarto(file = NULL)
}
\arguments{
\item{file}{Optional path to an already-downloaded
\verb{quarto-<version>-linux-amd64.tar.gz}, for offline installs. Default
\code{NULL} downloads the latest release from GitHub.}
}
\value{
The path to the installed \code{quarto} executable, invisibly.
}
\description{
Downloads the latest Quarto release and unpacks it under
\code{tools::R_user_dir("jekylldown", "data")/quarto}, where \code{\link[=build_site]{build_site()}},
\code{\link[=serve_site]{serve_site()}} and \code{\link[=check]{check()}} find it automatically. Nothing is installed
system-wide and no shell configuration is touched; uninstall by deleting
that directory. Only needed to write posts in Quarto (\code{.qmd}) --
see \code{\link[=new_post]{new_post()}}.
}
\details{
Linux x86_64 only for now (the platform jekylldown's isolated toolchain
targets); on other platforms install Quarto from
\url{https://quarto.org/docs/get-started/} and it is picked up from the
\code{PATH}.
}
\examples{
\dontrun{
install_quarto()

# offline: point at a tarball downloaded elsewhere
install_quarto(file = "~/Downloads/quarto-1.10.18-linux-amd64.tar.gz")
}
}
